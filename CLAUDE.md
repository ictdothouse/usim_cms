# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This directory is currently empty — the usim_cms project has not been scaffolded yet. There is no
build, lint, or test tooling to document, and no architecture to describe.

When the project is initialized (framework chosen, dependencies added, source files created), update
this file with:
- Setup/build/lint/test commands, including how to run a single test
- High-level architecture: major modules, data flow, and how they fit together
